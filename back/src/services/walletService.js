import { Wallet } from "../db/model/Wallet";

class walletService {
  //wallet 생성
  static async createWallet(userId) {
    const data = {
      userId: userId,
      krwAmount: 0,
      statement: null,
      expi,
    };
  }
}

export { walletService };
